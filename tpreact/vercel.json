{
    "version": 2,
    "builds": [
        {
        "src": "tpreact/public/**",
        "use": "@vercel/static"
        }
    ],
    "routes": [
        { "src": "/(.*)", "dest": "/tpreact/public/$1" }
    ]
}
